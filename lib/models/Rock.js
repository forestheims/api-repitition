const pool = require('../utils/pool');

module.exports = class Rock {
  id;
  name;
  mohs_hardness;
  composition;

  constructor(row) {
    this.id = row.id;
  }
  // CREATE

  // READ

  // UPDATE

  // DELETE
};
